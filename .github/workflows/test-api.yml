name: Test API
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  runs-on: ubuntu-latest
  strategy:
    matrix:
    
  steps:
    - uses: actions/checkout@v2
    - name: Use node version ${{ matrix.node-version }}